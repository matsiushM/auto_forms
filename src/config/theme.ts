import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        mode: 'light',
        background: {
            default: "#ffebeb",
        },
        secondary: {
            main: "#efffea"
        }
    }
});

export default theme;